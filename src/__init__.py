# This file serves as a marker, indicating to Python that it should be 
# treated as a package. This happens when setup.py is run;
# It looks for a __init__.py file in each folder.
# This allows module importation e.g. import package.module